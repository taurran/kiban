# Projects

Base for your code/repo workspaces. Organize however you like.

This folder lives inside the vault root but is **kept out of Obsidian's search and graph** (via
`.obsidian/app.json` → `userIgnoreFilters`) and **ignored by git** (except this README), because
code is not knowledge. Clone or scaffold repositories here without polluting your wiki graph.
